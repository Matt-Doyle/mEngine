// Development will be done in debug mode
// Worst case algorithm complexity for the majority of algorithms should be O(n log n), this is a game engine and handling complexities like O(n^2) may cause some framerate drops

// Windows
#include <Windows.h>

// IO
#include <cstdio>

// OpenGL

int APIENTRY WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow) { // Program entry point
	// SETUP

	// END OF SETUP

	return (0);
}